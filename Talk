Thank you, Amrutha. Good morning, everyone.

We just saw two possibilities with PNC connectors and skills. At C&IB, we built on that same foundation to create ARIA — our developer agent harness engineering platform.

Let me show you how ARIA simplifies development. ARIA is a terminal-first agent, and I'm going to give it a real task: build an API that fetches the last 90 days of micro-deposits, with pagination.

The moment I issue the prompt, ARIA looks up the relevant skills, templates, code samples and than create a plan.

In C&IB, we have strict guidelines: we maintain a standard folder structure, and every API must be protected via OAuth 2.0, with bearer-token validation based on the Ping certificate. We also have some additional capabilities — every API exposes a set of default endpoints, such as health check, version info, and synthetic monitoring.

These are all best practices we already follow in every development cycle. The difference is, now you do not have to remember them. Let ARIA carry them for you.

Let ARIA work for us.

Now, agentic skills are only useful if people can find them. So we built one more thing: the ARIA Skills Marketplace.
Think of it like an app store — but for ARIA agentic skills. It is one central place where every developer at PNC can go and see what is available.
In the marketplace, you can do five things:

One — discover skills.
Two — learn from other teams.
Three — give feedback and ratings.
Four — add your own skills with very little effort.
Five — reuse what already works, and spend your time on new problems instead.

Coming back to the ARIA agent demo.

[Show results]

The work ARIA just completed would typically take a developer one to two weeks. ARIA finished it in eight minutes — while I was talking to you.

And this is just the beginning. ARIA will keep learning, the marketplace will keep growing, and every skill we add makes all of us stronger.

Thank you.

Thank you all for your time with ARIA. Have a great day.
